(installation)=

# Installation

The package is published on [PyPI](https://pypi.org/project/camacq/) and can be installed with `pip` (or any equivalent):

```bash
pip install cam_acq
```

Next, see the {ref}`section about usage <usage>` to see how to use it.
